name: CI

on:
  # dev -> qa -> main. `dev` is where work lands and is pushed often: these are
  # the FAST checks, so a mistake surfaces in minutes. `qa` is the release
  # candidate — it runs these too, plus the expensive suite in qa.yml. `main`
  # only moves at release time, to a commit already green on qa.
  push:
    branches: [main, dev, qa]
  pull_request:

# freemkv-firmware's `freemkv-flash` crate path-deps ../../../libfreemkv (its
# `scsi` feature is the single source of truth for the per-OS SCSI backends),
# and libfreemkv in turn path-deps ../freemkv-unlock. actions/checkout refuses a
# `path:` outside $GITHUB_WORKSPACE, so every build job checks all three repos
# into sibling subdirectories and runs with `working-directory: freemkv-firmware`
# — the layout under which the path deps resolve exactly as they do locally.
env:
  CARGO_TERM_COLOR: always

jobs:
  # CI owns the changelog: main may never carry a version without a CHANGELOG.md
  # entry for it. Hard-fails on main (and PRs targeting main); warns elsewhere so
  # an in-progress dev/qa branch whose entry isn't written yet is not blocked.
  # Only reads Cargo.toml/CHANGELOG.md, so it needs no sibling checkout.
  changelog:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Require a CHANGELOG entry for the crate version on main
        shell: bash
        run: |
          set -euo pipefail
          version=$(grep -m1 '^version = ' Cargo.toml | sed -E 's/.*"([^"]+)".*/\1/')
          if grep -qE "^## \[${version}\]" CHANGELOG.md; then
            echo "CHANGELOG.md has an entry for ${version}."
            exit 0
          fi
          msg="CHANGELOG.md has no '## [${version}]' entry"
          if [ "${GITHUB_REF_NAME:-}" = "main" ] || [ "${GITHUB_BASE_REF:-}" = "main" ]; then
            echo "::error::${msg} — main requires a changelog entry for the released version."
            exit 1
          fi
          echo "::warning::${msg} (allowed on ${GITHUB_REF_NAME:-unknown}; required before it can reach main)."

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          path: freemkv-firmware
      - uses: actions/checkout@v7
        with:
          repository: freemkv/libfreemkv
          ref: "${{ github.ref_name == 'qa' && 'qa' || 'dev' }}"
          path: libfreemkv
      - uses: actions/checkout@v7
        with:
          repository: freemkv/freemkv-unlock
          ref: "${{ github.ref_name == 'qa' && 'qa' || 'dev' }}"
          path: freemkv-unlock
      - uses: dtolnay/rust-toolchain@1.94.0
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: freemkv-firmware
      - run: cargo fmt --all --check
        working-directory: freemkv-firmware
      # --all-targets so the test code is linted too, not just the libraries.
      - run: cargo clippy --all-targets --all-features -- -D warnings
        working-directory: freemkv-firmware

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          path: freemkv-firmware
      - uses: actions/checkout@v7
        with:
          repository: freemkv/libfreemkv
          ref: "${{ github.ref_name == 'qa' && 'qa' || 'dev' }}"
          path: libfreemkv
      - uses: actions/checkout@v7
        with:
          repository: freemkv/freemkv-unlock
          ref: "${{ github.ref_name == 'qa' && 'qa' || 'dev' }}"
          path: freemkv-unlock
      - uses: dtolnay/rust-toolchain@1.94.0
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: freemkv-firmware
      # Includes the CMAC-verify T0 proof against a committed stock image.
      - run: cargo test --tests
        working-directory: freemkv-firmware

  # The crate ships on macOS and Windows through libfreemkv's SCSI transport, so
  # it has to compile there. Build the tests (not just `cargo check`) to force
  # codegen on the cfg-gated platform halves. These run only on the
  # release-candidate branches — dev is the fast lane. `if` SKIPS the job (it
  # does not queue), so a release gate is never blocked by a never-scheduled job.
  check-macos:
    if: github.ref_name == 'qa' || github.ref_name == 'main'
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v7
        with:
          path: freemkv-firmware
      - uses: actions/checkout@v7
        with:
          repository: freemkv/libfreemkv
          ref: "${{ github.ref_name == 'qa' && 'qa' || 'dev' }}"
          path: libfreemkv
      - uses: actions/checkout@v7
        with:
          repository: freemkv/freemkv-unlock
          ref: "${{ github.ref_name == 'qa' && 'qa' || 'dev' }}"
          path: freemkv-unlock
      - uses: dtolnay/rust-toolchain@1.94.0
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: freemkv-firmware
      - run: cargo build --all-targets --all-features
        working-directory: freemkv-firmware

  check-windows:
    if: github.ref_name == 'qa' || github.ref_name == 'main'
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7
        with:
          path: freemkv-firmware
      - uses: actions/checkout@v7
        with:
          repository: freemkv/libfreemkv
          ref: "${{ github.ref_name == 'qa' && 'qa' || 'dev' }}"
          path: libfreemkv
      - uses: actions/checkout@v7
        with:
          repository: freemkv/freemkv-unlock
          ref: "${{ github.ref_name == 'qa' && 'qa' || 'dev' }}"
          path: freemkv-unlock
      - uses: dtolnay/rust-toolchain@1.94.0
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: freemkv-firmware
      - run: cargo build --all-targets --all-features
        working-directory: freemkv-firmware

  # ── Coverage (non-required, additive) ───────────────────────────────────────
  #
  # Reports line coverage to Codecov. Deliberately NOT wired into any release
  # gate: it is informational only and no other job `needs:` it. Mirrors the
  # `test` job's layout — same pinned 1.94 toolchain, same sibling checkout so
  # the path deps resolve. cargo-llvm-cov comes as a prebuilt binary from
  # taiki-e/install-action, so it is toolchain-independent.
  coverage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          path: freemkv-firmware
      - uses: actions/checkout@v7
        with:
          repository: freemkv/libfreemkv
          ref: "${{ github.ref_name == 'qa' && 'qa' || 'dev' }}"
          path: libfreemkv
      - uses: actions/checkout@v7
        with:
          repository: freemkv/freemkv-unlock
          ref: "${{ github.ref_name == 'qa' && 'qa' || 'dev' }}"
          path: freemkv-unlock
      - uses: dtolnay/rust-toolchain@1.94.0
        with:
          components: llvm-tools-preview
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: freemkv-firmware
      - uses: taiki-e/install-action@cargo-llvm-cov
      # Cover the library crates only. The egui/wgpu GUI bins carry no meaningful
      # unit coverage and building them under instrumentation is slow and brittle.
      - run: cargo llvm-cov --workspace --exclude freemkv-flash-gui --exclude freemkv-fw-gui --ignore-run-fail --lcov --output-path lcov.info
        working-directory: freemkv-firmware
      - uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: freemkv-firmware/lcov.info
          slug: freemkv/freemkv-firmware
          fail_ci_if_error: false
